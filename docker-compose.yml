# ⚠️  DO NOT USE THIS FILE
#
# This docker-compose.yml is intentionally disabled.
# The active one is one level up:
#
#   supertonic testing/docker-compose.yml
#
# Run from there:
#
#   cd ..
#   docker compose up --build
